{
  "name": "omdsh-desktop-runtime-pkg",
  "description": "Dependency-only deploy root for the desktop application's embedded harness runtime. Its dependencies are the published harness release the .app ships, the package manager that release shells out to, and whichever omdsh bundles the installer carries — none until they are published; 'pnpm run plugins:local' adds them. pnpm resolves the whole closure from the registry.",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "dependencies": {
    "@deepseek-ai/dsh": "0.1.0-rc.6",
    "pnpm": "11.21.0"
  }
}
